require 'rspec/expectations' 

Given /^I'm on the root page with a form containing two inputs named "(.*?)" and "(.*?)"$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

When /^I fill the form with user = "(.*?)", repo = "(.*?)" and submit it$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Then /^I should see a Google Map with markers representing commiters of the repo$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^Every markers should have an info window containing commiter's infos\.$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^All commiters who don't have any location informations should be listed under the map\. And Obviously every thing must be tested with Rspec \/ Capybara \/ VCR$/ do
  pending # express the regexp above with the code you wish you had
end